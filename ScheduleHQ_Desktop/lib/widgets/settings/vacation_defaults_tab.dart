import 'package:flutter/material.dart';

class VacationDefaultsTab extends StatelessWidget {
  const VacationDefaultsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Coming soon",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
